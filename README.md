# practice9
# fun-004 - Data Structures & Algorithms

## Overview

This assessment evaluates your understanding of core Python data structures and problem-solving techniques. You will implement a series of functions from scratch, increasing in difficulty.

Topics covered:
- Lists & Tuples
- Dictionaries
- Sorting & Searching
- Recursion
- Basic Algorithm Design

---

## Instructions

- Implement all functions in `main.py`
- Do NOT use built-in shortcuts unless explicitly allowed
- Ensure your code passes all tests in `test_file.py`
- Keep your code clean and readable

---

## Questions

### Question 1 - separate_even_odd(numbers)
Split a list into two lists:
- one with even numbers
- one with odd numbers

Example:
Input: [1, 2, 3, 4]
Output: ([2, 4], [1, 3])

---

### Question 2 - flatten_once(nested_list)
Flatten a list by ONE level only.

Example:
[[1, 2], [3, [4]]] → [1, 2, 3, [4]]

---

### Question 3 - index_map(items)
Create a dictionary mapping each item to its index.

---

### Question 4 - lowest_value_key(data)
Return the key with the lowest value.

---

### Question 5 - sort_by_last_letter(words)
Sort words based on their last character.

---

### Question 6 - categorize_products(products)
Group products by category.

---

### Question 7 - reverse_dict(data)
Swap keys and values.

---

### Question 8 - shift_left(nums, k)
Rotate list LEFT by k steps.

---

### Question 9 - sort_by_username(emails)
Sort emails based on username (before @).

---

### Question 10 - has_common_elements(list_a, list_b)
Return True if the lists share at least one element.

---

### Question 11 - safe_get(data, key)
Return value if key exists, otherwise return "Missing".

---

### Question 12 - cumulative_sum(n)
Return sum from 1 to n using recursion.

---

### Question 13 - custom_fibonacci(n)
Return first n Fibonacci numbers (starting from 1, 1).

---

## Theoretical Questions (No Coding)

1. What is the difference between a list and a tuple in Python?
2. What is the time complexity of dictionary lookup?
3. Explain recursion in your own words.
4. What is the difference between mutable and immutable data types?
5. When would you use a set instead of a list?
6. What does Big-O notation represent?
7. Why are dictionaries useful for lookups?
8. What is the difference between shallow copy and deep copy?
9. What happens if dictionary keys are duplicated?
10. Explain how sorting with a key function works.

---